# Lolykras Landing Sync

Landing page da Lolykras Consultoria (Vite + TypeScript + PNPM), com estrutura full-stack:
- `client/` → frontend (Vite/React)
- `server/` → backend
- `shared/` → código compartilhado

## Requisitos
- Node.js (recomendado 18+)
- PNPM

## Instalação
```bash
pnpm install
